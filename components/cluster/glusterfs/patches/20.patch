--- glusterfs-5.3/libglusterfs/src/compat-uuid.h.orig	2019-01-18 21:49:38.085120984 +0000
+++ glusterfs-5.3/libglusterfs/src/compat-uuid.h	2019-01-18 21:52:05.185504975 +0000
@@ -26,7 +26,11 @@
 }
 
 static inline void
+#ifdef GF_SOLARIS_HOST_OS
+gf_uuid_copy(uuid_t dst, uuid_t src)
+#else
 gf_uuid_copy(uuid_t dst, const uuid_t src)
+#endif
 {
     uuid_copy(dst, src);
 }
@@ -44,13 +48,21 @@
 }
 
 static inline int
+#ifdef GF_SOLARIS_HOST_OS
+gf_uuid_parse(char *in, uuid_t uuid)
+#else
 gf_uuid_parse(const char *in, uuid_t uuid)
+#endif
 {
     return uuid_parse(in, uuid);
 }
 
 static inline void
+#ifdef GF_SOLARIS_HOST_OS
+gf_uuid_unparse(uuid_t uuid, char *out)
+#else
 gf_uuid_unparse(const uuid_t uuid, char *out)
+#endif
 {
     uuid_unparse(uuid, out);
 }
