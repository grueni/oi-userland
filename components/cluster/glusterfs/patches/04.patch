--- glusterfs-5.3/libglusterfs/src/common-utils.c.orig	2019-01-17 22:14:10.218455936 +0000
+++ glusterfs-5.3/libglusterfs/src/common-utils.c	2019-01-17 22:16:09.581992643 +0000
@@ -5131,9 +5131,11 @@
      */
     for (;;) {
         int ngroups_old = ngroups;
-        ret = getgrouplist(user, group, *groups, &ngroups);
+//        ret = getgrouplist(user, group, *groups, &ngroups);
         if (ret != -1)
             break;
+        else
+            return -1;
 
         if (ngroups >= GF_MAX_AUX_GROUPS) {
             /*
